import { BriefcaseBusiness } from "lucide-react";
import SectionTitle from "./SectionTitle";
import SectionWrapper from "./SectionWrapper";
import SectionSubtitle from "./SectionSubtitle";

const Experiences = () => {
  return (
    <SectionWrapper>
      <SectionTitle>
        <BriefcaseBusiness />
        Professional Experiences
      </SectionTitle>
      <SectionSubtitle>
        Discover my professional journey, highlighting key roles, achievements,
        and the expertise I&apos;ve gained through diverse experiences.
      </SectionSubtitle>
    </SectionWrapper>
  );
};

export default Experiences;
